[
  {
    "id": "html-hero-layout",
    "category": "html-css",
    "title": "Center a Hero with Safe Areas",
    "description": "Responsive hero shell that respects safe areas and keeps content centered.",
    "tags": ["layout", "hero", "responsive"],
    "code": "<main class=\"min-h-screen flex items-center justify-center px-4 sm:px-6\">\n  <section class=\"max-w-3xl text-center space-y-4\">\n    <h1 class=\"text-3xl sm:text-4xl font-semibold tracking-tight\">Calm hero title</h1>\n    <p class=\"text-slate-500\">Subcopy that explains the value in one or two lines.</p>\n    <div class=\"flex flex-wrap items-center justify-center gap-3 pt-2\">\n      <button class=\"rounded-full bg-slate-900 text-white px-5 py-2 text-sm font-medium\">Primary</button>\n      <button class=\"rounded-full border border-slate-300 px-5 py-2 text-sm font-medium\">Secondary</button>\n    </div>\n  </section>\n</main>",
    "language": "html"
  },
  {
    "id": "js-debounce",
    "category": "javascript",
    "title": "Debounce Utility",
    "description": "Simple debounce helper for search inputs and resize handlers.",
    "tags": ["utilities", "performance"],
    "code": "export function debounce<T extends (...args: any[]) => void>(fn: T, delay = 250) {\n  let timeout: number | undefined;\n  return (...args: Parameters<T>) => {\n    window.clearTimeout(timeout);\n    timeout = window.setTimeout(() => fn(...args), delay);\n  };\n}",
    "language": "ts"
  },
  {
    "id": "react-use-toggle",
    "category": "react",
    "title": "useToggle Hook",
    "description": "Tiny hook for boolean UI state (modals, dropdowns, etc.).",
    "tags": ["hooks", "state"],
    "code": "import { useCallback, useState } from 'react';\n\nexport function useToggle(initial = false) {\n  const [on, setOn] = useState(initial);\n\n  const toggle = useCallback(() => setOn((prev) => !prev), []);\n  const open = useCallback(() => setOn(true), []);\n  const close = useCallback(() => setOn(false), []);\n\n  return { on, toggle, open, close };\n}",
    "language": "ts"
  },
  {
    "id": "boilerplate-api-layout",
    "category": "boilerplates",
    "title": "API Page Layout",
    "description": "Skeleton layout for pages that fetch and render remote data.",
    "tags": ["layout", "api", "loading"],
    "code": "export function ApiPageLayout({ state }: { state: 'idle' | 'loading' | 'error' | 'success' }) {\n  if (state === 'loading') return <p>Loading…</p>;\n  if (state === 'error') return <p>Something went wrong. Try again.</p>;\n  if (state === 'idle') return <p>Search or trigger a request to begin.</p>;\n  return <div>/* render your successful data state */</div>;\n}",
    "language": "tsx"
  }
]
